const form=document.getElementById('loginForm');const msg=document.getElementById('loginMessage');const next=new URLSearchParams(location.search).get('next');form.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='جاري تسجيل الدخول...';msg.className='form-message';try{const cred=await auth.signInWithEmailAndPassword(document.getElementById('email').value.trim(),document.getElementById('password').value);const snap=await db.collection('users').doc(cred.user.uid).get();if(snap.exists&&snap.data().role==='admin')location.href=next||'admin.html';else location.href=next&&next!=='admin.html'?next:'member.html';}catch(err){console.error(err);msg.textContent=(err.code==='auth/invalid-credential'||err.code==='auth/wrong-password'||err.code==='auth/user-not-found')?'البريد الإلكتروني أو كلمة المرور غير صحيحة.':'حدث خطأ أثناء تسجيل الدخول. حاول مرة أخرى.';msg.className='form-message error';}});