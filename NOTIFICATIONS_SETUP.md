# إعداد الإشعارات - فريق شمال واسط

## الحالة الحالية

المشروع يحتوي على نظام إشعارات داخل التطبيق باستخدام Firestore Realtime Listeners.

### إشعارات المشرفين

عند وصول استفسار مواطن جديد:
- يظهر عداد الإشعارات في لوحة الإدارة.
- يتم تحديث العداد بشكل لحظي.
- عند فتح الاستفسارات من لوحة الإدارة يتم اعتبار الإشعار مقروءًا.

### إشعارات الأعضاء

عند قيام الإدارة بالرد على استفسار العضو:
- يظهر إشعار للعضو.
- يظهر عدد الردود غير المقروءة.
- عند فتح سجل الاستفسارات يتم اعتبار الردود مقروءة.

---

# الملفات المسؤولة عن الإشعارات

## js/notifications.js

يحتوي على منطق الإشعارات داخل التطبيق.

المهام الأساسية:

- مراقبة الاستفسارات الجديدة للمشرفين.
- مراقبة الردود الجديدة للأعضاء.
- تحديث عداد الإشعارات.
- تعليم الإشعارات كمقروءة.

## js/app.js

مسؤول عن تشغيل Service Worker الخاص بالموقع.

## sw.js

Service Worker الخاص بتطبيق PWA.

حاليًا يستخدم من أجل:

- تخزين ملفات التطبيق الأساسية مؤقتًا.
- تحسين تشغيل الموقع عند ضعف الاتصال.
- تجهيز المشروع لدعم Push Notifications مستقبلًا.

---

# Firebase Cloud Messaging

الإشعارات التي تظهر داخل الموقع تعمل حاليًا باستخدام Firestore.

أما الإشعارات الحقيقية على الهاتف، مثل:

> وصول إشعار إلى الهاتف والموقع مغلق

فتحتاج إلى إعداد Firebase Cloud Messaging (FCM).

---

# متطلبات Push Notifications

لتفعيل الإشعارات على الهاتف نحتاج إلى:

1. Firebase Cloud Messaging.
2. Web Push / VAPID Key.
3. Service Worker يدعم FCM.
4. Backend أو Firebase Cloud Functions لإرسال الإشعارات.
5. حفظ FCM Token الخاص بكل عضو.

---

# VAPID Key

من Firebase Console يتم إنشاء Web Push Certificate.

المسار:

Firebase Console
→ Project Settings
→ Cloud Messaging
→ Web configuration
→ Web Push certificates

يتم إنشاء أو استخدام VAPID Key.

لا تضع مفتاح VAPID السري داخل هذا الملف.

---

# Firebase Cloud Functions

يمكن استخدام Cloud Functions لإرسال الإشعارات تلقائيًا.

أمثلة:

### عند إنشاء استفسار جديد

يتم إرسال إشعار إلى المشرفين.

### عند رد المشرف على استفسار

يتم إرسال إشعار إلى صاحب الاستفسار.

---

# ملاحظات الأمان

لا يتم وضع:

- Firebase Admin SDK credentials
- Service Account private key
- أي مفتاح سري

داخل ملفات JavaScript الموجودة في الموقع.

أي مفاتيح سرية يجب أن تبقى في Backend / Cloud Functions.

---

# الحالة في Phase 7

تم تجهيز:

- نظام الإشعارات داخل التطبيق.
- عدادات الإشعارات.
- مراقبة الاستفسارات الجديدة.
- مراقبة الردود الجديدة.
- تعليم الإشعارات كمقروءة.
- Service Worker.
- أساس PWA.

ولم يتم تفعيل Push Notifications للهاتف بشكل كامل بعد.

---

# الخطوة المستقبلية

في المرحلة القادمة يمكن إضافة:

- Firebase Cloud Messaging.
- إشعارات الهاتف الحقيقية.
- حفظ FCM Tokens.
- إرسال إشعار عند وصول استفسار جديد.
- إرسال إشعار عند الرد على الاستفسار.
- إمكانية التحكم بالإشعارات من حساب العضو.

---

## مهم

هذا الملف (`NOTIFICATIONS_SETUP.md`) هو ملف توثيق وشرح فقط.

لا يتم استدعاؤه من صفحات HTML ولا يحتاج إلى إضافته داخل `<script>`.

الملفات البرمجية الفعلية الخاصة بالنظام هي:

- `js/notifications.js`
- `js/app.js`
- `sw.js`